using UnityEngine;
using System.Collections;

public class destroCreditsScript : MonoBehaviour {
private bool open = true;

	/*
void Update ()
	{
		open = GameObject.Find("creditsButton").GetComponent<creditScript>().creditsOpen;
		if (!open) {
		Destroy (gameObject);
		}
	}
}
*/
}